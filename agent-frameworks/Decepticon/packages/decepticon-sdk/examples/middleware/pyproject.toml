[project]
name = "decepticon-example-middleware"
version = "0.0.1"
description = "Decepticon plugin scaffolded by decepticon-sdk."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.13"

dependencies = [
    "decepticon-sdk",
]

[project.entry-points."decepticon.middleware"]
decepticon_example_middleware = "decepticon_example_middleware"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/decepticon_example_middleware"]
