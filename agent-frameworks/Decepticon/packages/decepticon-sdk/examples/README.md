# decepticon-sdk examples

Six runnable example plugins, one per supported plugin kind. Each
directory is a buildable PyPI-style plugin package generated by
``decepticon-sdk plugin new``.

| Directory | Kind | Entry-point group |
|-----------|------|-------------------|
| [`tool/`](tool/) | tool | `decepticon.tools` |
| [`middleware/`](middleware/) | middleware | `decepticon.middleware` |
| [`agent/`](agent/) | agent | `decepticon.agents` |
| [`callback/`](callback/) | callback | `decepticon.callbacks` |
| [`skill/`](skill/) | skill | `decepticon.skills` |
| [`prompt/`](prompt/) | prompt | `decepticon.prompts` |

## Try one

```bash
cd middleware
uv build
pip install dist/*.whl

python -c "from importlib.metadata import entry_points; print(entry_points(group='decepticon.middleware'))"
```

## Regenerate

All six were generated by the SDK scaffolder. To regenerate (or to
scaffold your own), run from the workspace root:

```bash
uv run decepticon-sdk plugin new \
    --kind=middleware \
    --name=my-plugin \
    --path=./my-plugin
```

See the umbrella spec at
[`docs/superpowers/specs/2026-05-23-core-framework-sdk-split-design.md`](../../../docs/superpowers/specs/2026-05-23-core-framework-sdk-split-design.md)
§6.3 for the SDK design rationale and §14 #7 / #8 for the acceptance
criteria these examples satisfy.
