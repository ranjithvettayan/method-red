[project]
name = "decepticon-example-tool"
version = "0.0.1"
description = "Decepticon plugin scaffolded by decepticon-sdk."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.13"

dependencies = [
    "decepticon-sdk",
]

[project.entry-points."decepticon.tools"]
decepticon_example_tool = "decepticon_example_tool"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/decepticon_example_tool"]
