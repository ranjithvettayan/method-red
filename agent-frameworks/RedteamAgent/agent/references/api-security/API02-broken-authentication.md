# API2:2023 — Broken Authentication

- Test for missing auth on API endpoints
- Check token validation: expired, tampered, empty tokens
- Test API key rotation and revocation
- Check for auth bypass via HTTP method switching
- Related skills: `auth-bypass`
